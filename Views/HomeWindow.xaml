<Window x:Class="PRN212.Views.HomeWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:local="clr-namespace:PRN212.Views"
        mc:Ignorable="d"
        Title="HomeWindow" Height="auto" Width="1000">
    <Grid Background="#1E90FF"> <!-- Màu nền xanh dương -->
        <!-- Hình ảnh minh họa giao thông (thay thế bằng các hình ảnh thực tế) -->
        <StackPanel VerticalAlignment="Center" HorizontalAlignment="Center">
            <Image Source="car.png" Width="100" Height="100" Margin="10" />
            <Image Source="road.png" Width="150" Height="100" Margin="10" />
            <Image Source="map.png" Width="120" Height="120" Margin="10" />
        </StackPanel>

        <!-- Danh sách các nút điều hướng -->
        <StackPanel VerticalAlignment="Center" HorizontalAlignment="Left" Margin="20,0,0,0">
            <Button Content="Manage Vehicles" Width="200" Height="50" Margin="0,10" Background="#FFA500" Foreground="White" FontSize="16"/>
            <Button Content="Manage Routes" Width="200" Height="50" Margin="0,10" Background="#FFA500" Foreground="White" FontSize="16" />
            <Button Content="Manage Traffic" Width="200" Height="50" Margin="0,10" Background="#FFA500" Foreground="White" FontSize="16" />
            <Button Content="Manage Drivers" Width="200" Height="50" Margin="0,10" Background="#FFA500" Foreground="White" FontSize="16" />
            <Button Content="Manage Stations" Width="200" Height="50" Margin="0,10" Background="#FFA500" Foreground="White" FontSize="16"/>
            <Button Content="Manage Schedules" Width="200" Height="50" Margin="0,10" Background="#FFA500" Foreground="White" FontSize="16" />
            <Button Content="Manage Account" Width="200" Height="50" Margin="0,10" Background="#FFA500" Foreground="White" FontSize="16" />
            <Button Content="Admin Dashboard" Width="200" Height="50" Margin="0,10" Background="#FFA500" Foreground="White" FontSize="16" />
        </StackPanel>

        <!-- Nút Logout -->
        <Button Content="Logout" Width="100" Height="40" Background="#FFA500" Foreground="White" FontSize="14" 
                HorizontalAlignment="Right" VerticalAlignment="Top" Margin="0,10,20,0"/>
    </Grid>
</Window>
